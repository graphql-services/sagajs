{
  "name": "saga",
  "private": true,
  "devDependencies": {
    "@types/node": "^10.12.0",
    "lerna": "^3.4.3",
    "typescript": "^3.1.3"
  },
  "scripts": {
    "publish": "lerna run tsc && lerna publish",
    "publish-canary": "lerna run tsc && lerna publish -c"
  }
}
